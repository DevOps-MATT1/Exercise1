# This is the workflow name
name: 01-BuildingBlock

# ON - this is trigger
on: push

jobs:
  echo-hello:
    runs-on: ubuntu-latest
    steps:
    - name: Say hello
      run: echo "Hello, world!"
  echo-goodbye:
      runs-on: ubuntu-latest
      steps:
      - name: Failed Step hello
        run: 
          echo "Hello, world!"
          exit 1
      - name: Say goodbye
        run:  echo "Goodbye, world!"
               

    
